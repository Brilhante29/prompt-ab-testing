# Benchmark Plan

Primary metric: `best_variant_score`.

Command:

```powershell
python -m prompt_ab_testing benchmark --output benchmarks/results/prompt-ab-baseline.json
```

The benchmark uses local fixtures so the result is reproducible and does not require external credentials.
